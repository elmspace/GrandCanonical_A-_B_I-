/*
  This file is simple, it allocates memory, sets the parameters,
  and initial fields, and calculates the free energy
*/
using namespace std;

void Mod1(){
  
  Allocate(1);
  Parameters( );
  omega( );
  FreeEnergy( );
  Allocate(-1);
 
}
